@model Web_CinemaManagement.Models.ModelLinq.DANGPHIM

@{
    ViewBag.Title = "Xóa Dạng Phim";
    Layout = "~/Areas/Manager/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid" style="padding-top: 20px;">
    <div class="panel panel-danger">
        <div class="panel-heading">
            <h3 class="panel-title">@ViewBag.Title</h3>
        </div>
        <div class="panel-body">
            <h3>Bạn có chắc chắn muốn xóa dạng phim này?</h3>
            <p class="text-danger">Lưu ý: Nếu có phim đang sử dụng dạng phim này, việc xóa có thể thất bại.</p>
            <div>
                <dl class="dl-horizontal">
                    <dt>Mã Dạng Phim</dt>
                    <dd>@Html.DisplayFor(model => model.MADP)</dd>

                    <dt>Tên Dạng Phim</dt>
                    <dd>@Html.DisplayFor(model => model.DANGPHIM1)</dd>

                    <dt>Trạng Thái</dt>
                    <dd>@Html.DisplayFor(model => model.TrangThai)</dd>
                </dl>
                <hr />

                @using (Html.BeginForm("DeleteDangPhim", "Movies", new { area = "Manager", id = Model.MADP }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-actions no-color">
                        <input type="submit" value="Xác nhận Xóa" class="btn btn-danger" />
                        @Html.ActionLink("Quay lại danh sách", "Index", "Movies", new { area = "Manager" }, new { @class = "btn btn-default" })
                    </div>
                }
            </div>
        </div>
    </div>
</div>